namespace Shared
{
    /// <summary>
    /// This is a shared class. This class is accessable in both server and client.
    /// You can also make use of the fivem shared models like Vector3 etc.
    ///
    /// Tip. If you want to have a shared class. But want the client(or just the server) to some more data. Make this class partial and extend the class further in the client or server.
    /// </summary>
    class SharedClassOrSomething
    {
    }
}